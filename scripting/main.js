const options = {
  strings: ["Designer", "Developer"],
  typeSpeed: 50, 
  backSpeed: 25,
  loop: true, 
  backDelay: 500, 
  startDelay: 500,
};

const typed = new Typed(".typed-element", options);
